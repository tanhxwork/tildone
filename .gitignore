# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Private dev docs — tracked by the .gitprivate overlay repo (tildone-dev),
# never by this public repo
CLAUDE.md
.claude/
docs/
.gitprivate/
.impeccable/

# per-worktree generated dev icons (scripts/tauri.sh); the committed red set stays
src-tauri/icons-dev/*/

# design-sync (dev tooling — tracked in the private repo)
.design-sync/
.ds-sync/
ds-bundle/
